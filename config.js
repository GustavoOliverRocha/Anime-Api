/**
 * Configuração da base de dados
 * 
 * */
const config_host = 'localhost',
	  config_database = 'test',
	  config_user = 'root',
	  config_password = '';

module.exports = {config_host,config_database,config_user,config_password};